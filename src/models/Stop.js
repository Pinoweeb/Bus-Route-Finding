class Stop {
  constructor(id, name, lat, lon) {
    this.id = id;       // stop_id
    this.name = name;   // stop_name
    this.lat = lat;     // stop_lat
    this.lon = lon;     // stop_lon
  }
}

module.exports = Stop;
